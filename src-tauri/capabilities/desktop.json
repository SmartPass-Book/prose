{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions. The updater plugin isn't linked on mobile (updates there go through the App Store), and window dragging backs the custom overlay titlebar, which iOS has no equivalent of.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["core:window:allow-start-dragging", "updater:default"]
}
